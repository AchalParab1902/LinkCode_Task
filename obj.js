// object = object is a collection of key-value pairs.
// Keys (also called properties) are strings (e.g., "name", "age").
// Values can be any data type (numbers, strings, arrays, functions, even other objects).

const person = {
    name: "Achal",
    age: 25,
    address :{
        city:"Pune"
    
    }

};
console.log(person);
console.log(person.name);
console.log(person["age"]);
console.log(person["address"]);

